{% extends 'baseadmin.html.twig' %}

{% block title %}Findemp{% endblock %}

{% block body %}
    <h1>Findemp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ findemp.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ findemp.nom }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ findemp.domaine }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ findemp.email }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ findemp.numero }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ findemp.place }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_findemp_index') }}">back to list</a>

    <a href="{{ path('app_findemp_edit', {'id': findemp.id}) }}">edit</a>

    {{ include('findemp/_delete_form.html.twig') }}
{% endblock %}
