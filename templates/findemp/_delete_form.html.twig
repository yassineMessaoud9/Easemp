<form method="post" action="{{ path('app_findemp_delete', {'id': findemp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ findemp.id) }}">
    <button class="btn">Delete</button>
</form>
